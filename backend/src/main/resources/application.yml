spring:
  datasource:
    username: ${DB_USER_NAME}
    password: ${DB_PASSWORD}
    url: ${DB_URL}
    driver-class-name: com.mysql.cj.jdbc.Driver
#  redis:
#    host: localhost
#    port: 6379
#  jpa:
#    show-sql: true
#    hibernate:
#      ddl-auto: update
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

security:
  jwt:
    header: ${JWT_AUTH:Authorization}
    secret: ${JWT_SECRET:superCoding}
    token-validity-in-seconds: 604800
  oauth2:
    client:
      registration:
        google:
          client-id: 732457214657-04h0pufj3ga4mlh9r6p6sfb88hvapgbi.apps.googleusercontent.com
          client-secret: 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
          redirect-uri: http://localhost:8080/login/oauth2/code/google
          authorizationUri: https://accounts.google.com/o/oauth2/auth
          scope: email, profile

#aws:
#  accessKeyId: ${AWS_ACCESS_KEY}
#  secretKey: ${AWS_SECRET_KEY}
#  region: ${AWS_REGION}
#  s3:
#    bucketName: ${AWS_BUCKET}

logging:
  level:
    org.springframework.security: DEBUG
